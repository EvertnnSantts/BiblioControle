# ===================================================
# BiblioControle — Variáveis de Ambiente do Cliente
# ===================================================
# Em desenvolvimento: deixe vazio ou aponte para localhost
# Em produção (Vercel): configure com a URL do backend deployado

# URL base da API (sem barra final)
# Dev: http://localhost:5000   ← proxy do Vite assume o resto
# Prod: https://seu-backend.railway.app  ← URL real do serviço
VITE_API_URL=http://localhost:5000
